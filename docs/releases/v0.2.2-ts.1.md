# TokenSupply v0.2.2-ts.1

## Highlights

- 追上上游 `v0.2.2`（102 个上游提交，PR #172 merge 合入，merge-base 推进至 `ab99d56e9`，`behind` 回零）：**分组模型白名单**（`models_list_config` → `model_allowlist`，语义从「仅影响 /v1/models 展示」升级为同时约束模型列表与请求准入）、Codex manifest 固定账号一系列修复、GPT-6 Astra 正确性修复批次、OpenAI 流式失败上游诊断与 model-not-found failover、Claude CLI 版本下限 2.1.259、i18n 构建门禁。
- 用户账号（ssharkkky）6 个上游已合并 PR 随 upstream 历史一并纳入（fork cherry-pick 闭环）：#6669 redeem 固定失败窗口、#6673 备份迁移咨询锁、#6670 支付履约隔离、#6668 channel monitor gemini role、#6665 grok 跨客户端映射默认文档、#6667 antigravity user-agent 版本。
- Fork 行为全部保留：Kiro 平台（缓存仿真/auto-sticky/端点模式/`kiro_credits`）、渠道货架 storefront（账号 mapping 不成公开货架）、prompt-audit 回退分组、镜像任务接口、Grok ≥200K 长上下文计费回填、按供应商支付对账。

## Fork Changes

- 自上一完成版本 `v0.2.1-ts.1`（`c16537e72`）以来 fork 侧变更：
  - PR #172 `e36f5b859` chore(sync)：上游 v0.2.2 同步（28 冲突文件逐项裁决，merge commit 保留 upstream parent；本地 unit 66 包 / integration 59 包 Testcontainers / `golangci-lint` 0 issues / 前端 301 文件 2124 用例全绿）。关键 fork 行为保留：Kiro 全部字段与模拟缓存、渠道货架（无 allowlist 时账号 mapping keys 不成货架，回落 fork 扩展默认目录）、`fallback_group_id_on_prompt_audit_block` simple-mode 绑定策略、镜像任务接口 `GET /images/tasks/:task_id/images/:image_index`、Grok 长上下文回填、affiliate 余额履约接线。冲突裁决要点：`apiKeyAuthSnapshotVersion` 24 → 25（双方各自新增缓存字段）；`/v1/models` 与 Codex/Gemini 列表在 allowlist 启用时统一走 `FilterForListing`（allowlist 源 = 渠道货架 ∪ 账号 mapping keys ∪ 平台默认，Anthropic 侧 merge，保留 fork「无 allowlist 时 mapping 不成货架」语义）；`redeem_service.go` 双方同名函数去重取上游实现；`payment_fulfillment_test` 上游 stub + fork affiliate 断言；`backup_pg_dumper_test` 上游辅助 + fork 迁移锁 3 测试；image playground 专属判定恢复 `len(Models) > 0` 空列表保护；i18n 并集并补齐 `common.retry` / `admin.accounts.add`（上游新 i18n 完整性门禁检出 fork 引用缺 key）；`ent/*` 与 `wire_gen.go` 重新生成（ent 保留上游 allowlist + fork Kiro/channel 字段）。
  - 迁移编号：上游 `235_group_model_allowlist.sql`（裸 `RENAME COLUMN`）按 fork 规则转为 `275_group_model_allowlist.sql`（fork 上版最大编号 274 之后），改为 expand/contract 回滚安全形态。
- 既有 fork 能力全部保留并经回归：Kiro 平台、分组/渠道定价、service-tier / OAuth fast-tier 计费权威、提示词审计、渠道监控 v2、长上下文统一计费、分组 Fast / Free Fast 开关、reasoning-effort 超限控制、1h 缓存写入计价、仓库模型目录 10min 热生效。

## Upstream Changes

- Baseline: `upstream/ab99d56e9`（v0.2.1） -> `upstream/b7dba6267`（v0.2.2），共 102 个提交。
- **分组模型白名单**（#6568/#6658）：`groups.models_list_config` 更名 `model_allowlist`，白名单同时约束模型列表接口与请求准入；候选/通配符展开、管理 UI 重命名（`modelAllowlist.*`）、ent schema 重生成；未启用时行为与旧版一致。
- **Codex manifest 固定账号**：reactive v-model 绑定修复（#6643）、pinned-accounts 修复（#6662）、failover alias（#6688）、allowed-tools 选择约束（#6733）、Codex OAuth 保留 Astra reasoning mode。
- **GPT-6 Astra**：instructions 正确选择（#6690）、stale input modalities 修复（#6678）、ultra catalog（#6718）。
- **OpenAI**：mixed-account 模型列表修复（#6680）、流式失败上游诊断信息（#6736）、reasoning-effort 映射支持拒绝转发值（#6726）、model-not-found failover（#6711）。
- **计费/定价**：Gemini flash thinking 档位目录价（#6637）、GLM-5.3-flash 回退定价（#6429）、DeepSeek 账号成本峰值修复（#6700）、interval multiplier 显示修复（#6738）。
- **网关/平台**：Claude thinking block 绑定（#6677）、antigravity toolconfig 始终存在（#6674）、Claude CLI 版本下限 2.1.259（#6481）、WS pending-turn 终态（#6498）、WS 轮次限流（#6708）、HTTP bridge 会话隔离（#6704）、Grok 媒体 inconclusive 资格判定（#6486）、WS passthrough-before-turn（#5453）、function arguments 保留（#6629）、native delegation WS（#6687）、message cache JSON 转义（#6702）。
- **i18n 构建门禁**（#6684）：locale key 完整性 Vitest（EN/ZH schema 一致、叶子非空、静态引用全覆盖）进入构建。
- **管理与运维**：onboarding 按钮阴影（#312）、turnstile loading（#3667）、usage API key 过滤（#4827）、订阅使用链接（#6723）、账户过期预设（#275）、plaza 定价列宽自适应。
- **用户账号上游 PR（fork cherry-pick）**：#6669 redeem 固定失败窗口、#6673 备份迁移咨询锁、#6670 支付履约隔离、#6668 channel monitor gemini role、#6665 grok 跨客户端映射默认文档、#6667 antigravity user-agent 版本。

## Configuration And Migrations

- 新增迁移 1 个（additive，回滚安全）：
  - `275_group_model_allowlist.sql`：上游原 235（裸 `RENAME COLUMN`）按 fork 规则重编号并展开为 expand/contract 形态：① `groups.model_allowlist JSONB` 可空新列（旧二进制继续按旧列工作）；② 一次性回填 `model_allowlist = models_list_config`（两列 JSON 结构一致 `{"enabled":bool,"models":[]}`，历史配置不丢失）；③ `BEFORE INSERT/UPDATE OF model_allowlist` 触发器在 DB 边界归一化旧二进制的 NULL 写入；④ 旧列 `models_list_config` 保留（contract 步骤留待后续版本）。无 DDL 数据丢弃、无表重写。
  - 注意：后续迁移请从 276 开始编号。
- 行为变化（无需配置动作，但需知悉）：已启用且配置了 models 的分组，其白名单从「仅影响模型列表展示」升级为**同时约束请求准入**——列表外模型将被拒绝（上游语义升级，白名单是显式管理员配置，默认未启用）。
- 配置：无新增配置项；`deploy/config.example.yaml` 无变化。
- 鉴权快照版本 24 → 25（内部缓存版本号，自动失效重建，无需运维动作）。

## Deployment And Rollback

- 蓝绿更新即可，无需停机：先跑迁移 275（additive：可空新列 + 仅 NULL 行回填 + 触发器，`ADD COLUMN` 元数据级无锁表），再滚动新镜像。
- 上一完成版本 `v0.2.1-ts.1` 保持回滚兼容：旧列 `models_list_config` 全程保留且有数据（回填 + 触发器归一化），旧版本二进制回滚后继续按旧列读写；回滚后 v0.2.2 上游能力（白名单准入、Codex manifest 修复、Astra 修复等）不可用，既有计费流水与用量记录不受影响。
- 正式 tag 只能由 `Promote Release` workflow 在精确 `main` SHA 通过所有门禁后创建，不手工创建或覆盖；`main` 从本 Release PR 合并到 Promote 完成期间冻结（≤7 天）。

## Verification

- 纳入 PR 验证（以 PR 记录为准）：#172 分支本地 `go build/vet` 干净、`go test -tags=unit` 66 包全绿、`go test -tags=integration`（真实 PostgreSQL/Redis Testcontainers）59 包全绿（含 migration history/upgrade/rollback compatibility 与 allowlist 投影测试）、`golangci-lint` 0 issues、`go mod tidy` 无漂移、前端 `vue-tsc --noEmit` / `eslint` / Vitest 301 文件 2124 用例全绿；PR CI 在最终 SHA `e36f5b859` 上 backend test（unit + integration）、frontend、`golangci-lint`、backend-security、frontend-security、release-safety、shell、Release Check、Release Preflight 全部 SUCCESS。
- 迁移审计结论（高风险路径 `migrations/`，结论全文见 PR #172「迁移」节）：仅追加可空列 + 数据原样回填 + 归一化触发器，无数据丢弃/表重写/锁表；回滚 = 镜像回退（旧列数据完整）。残余风险：混合版本窗口内旧二进制对新列写入不可见（与 234 迁移同型的已知可接受窗口）。
- 本 Release PR 自身仅改 `VERSION` 与版本化 notes（`Release Check` 轻量门禁：diff 包含性、notes 校验、版本递增、tidy、branch protection、高风险路径机械判定；命中 `migrations/`，审计结论链接指向 PR #172）。
- 合并后的精确 `origin/main` SHA：`Backend CI` push run 真跑全量，或 `verify-sha-evidence` 判定证据链成立（fail-closed）而跳过；`Release Preflight` 的 `build-candidate` 真实产出 candidate 工件（VERSION `0.2.2-ts.1` 且 `TARGET_SHA == origin/main` tip）并通过全链路验证；任一门禁失败回到修复 PR，在新 SHA 全绿前不创建正式 tag。

## Known Limitations

- 白名单语义升级为准入约束是行为变化：启用 allowlist 的分组会开始拒绝列表外模型（显式管理员配置，默认未启用，未启用分组零影响）。
- 旧列 `models_list_config` 保留至后续 contract 版本；期间混合版本窗口内旧二进制对新列写入不可见（同 234 迁移型已知窗口）。
- 上游原 235 迁移编号与 fork 既有迁移冲突，已按 267–274 既有惯例续接重命名为 275（内容等价，形态按 fork 回滚安全惯例展开）；后续迁移从 276 编号。
- 无 tag 的 `go test ./...` 不含 `unit`/`integration` tag 测试文件；同步/发布验证以 `make test-unit` / `make test-integration` 与 Actions 为准。
